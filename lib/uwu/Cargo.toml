[package]
name = "uwu"
version = "0.1.0"
authors = ["Ivan Tham <pickfire@riseup.net>"]
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
uwuify = { version = "0.2.2", default-features = false, features = ["owo-colors"] }
